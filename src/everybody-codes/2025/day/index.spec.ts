import { part_1, part_2, part_3 } from './index.ts';

describe('Day', () => {
  it('part 1 example', () => {
    expect(part_1()).toBe(0);
  });

  it('part 1 input', () => {
    expect(part_1()).toBe(0);
  });

  it('part 2 example', () => {
    expect(part_2()).toBe(0);
  });

  it('part 2 input', () => {
    expect(part_2()).toBe(0);
  });

  it('part 3 example', () => {
    expect(part_3()).toBe(0);
  });

  it('part 3 input', () => {
    expect(part_3()).toBe(0);
  });
});

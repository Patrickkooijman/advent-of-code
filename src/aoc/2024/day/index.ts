const parseInput = (data: string): any => {
    console.log(data)
};

export const part_1 = (data: string): number => {
    parseInput(data);

    return 0;
};

export const part_2 = (data: string): number => {
    parseInput(data);

    return 0;
};
